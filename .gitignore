# ---- Build output ----
bin/
obj/
[Dd]ebug/
[Rr]elease/
*.user
*.suo

# ---- IDE / editor ----
.vs/
.vscode/
.idea/
*.userprefs

# ---- Test output ----
[Tt]est[Rr]esults/
*.trx
coverage*.xml
coverage*.json

# ---- Local SQLite database (created at runtime via EF Core migrations) ----
*.db
*.db-shm
*.db-wal

# ---- Data Protection key ring (encrypts the saved secrets; machine-scoped) ----
.dpkeys/

# ---- Secrets saved via the Settings page (encrypted, per-machine) — never commit ----
.githubtoken
.anthropickey
.openaikey
.aiprovider

# ---- OS cruft ----
.DS_Store
Thumbs.db

# ---- Internal planning docs (kept local, not published) ----
/CLAUDE.md
/ROADMAP.md
/STATUS.md
