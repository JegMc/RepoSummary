# docs — screenshots & images

Drop screenshots and images for the README here.

The main [`README.md`](../README.md) references these files (uncomment the image tags there
once you add them):

| File | Suggested shot |
|------|----------------|
| `analysis.png`     | The analysis page for a repo — repo header + maturity scorecard |
| `diagram.png`      | The Evidence tab's architecture / ER diagram |
| `generate.png`     | Generated résumé/interview material with evidence tags |
| `ask.png`          | The "Ask this repo" tab answering a question |

**Tips**
- PNG, ~1400px wide, taken in light mode for consistent contrast.
- Keep each file under ~500 KB so the repo stays lean (crop tightly; compress if needed).
- Reference them with a relative path, e.g. `![Analysis page](docs/analysis.png)`.
